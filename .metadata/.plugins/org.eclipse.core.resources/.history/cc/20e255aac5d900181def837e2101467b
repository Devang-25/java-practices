package com.preety.chessboard.domain;

import com.preety.chessboard.domain.util.BoardUtil;

public class Queen extends Piece{
	private Color color; 
	
	public Queen(Color color) {
		super(color, PieceType.Q);
		this.color = color;
	}

	@Override
	public void move(Position start, Position end) {
		//System.out.println(new Queen(Color.W).getPiece());
	//	System.out.println(new Queen(Color.W).getColor());
		
		if(BoardUtil.IsOccupied(end)) {
			
		}
	}

}
